# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs (will be built in Docker)
dist/

# Environment files (will be provided via environment variables)
.env
.env.*
!.env.example

# Git
.git/
.gitignore

# IDE
.vscode/
.idea/
*.sublime-*

# Logs
logs/
*.log

# Testing
coverage/
.nyc_output/
*.lcov
.jest/

# OS
.DS_Store
Thumbs.db

# Docker (keep Dockerfile and docker-compose.yml for build context)
# Note: Dockerfile and docker-compose.yml are needed, so we don't ignore them
.dockerignore

# Documentation
*.md
!README.md

# Deployment scripts (not needed in container)
deploy/
scripts/
nginx/
systemd/

# Temporary files
tmp/
temp/
*.tmp
.cache/

# Uploads (will be mounted as volume)
uploads/*

# PM2 (not needed in Docker)
ecosystem.config.js
.pm2/

